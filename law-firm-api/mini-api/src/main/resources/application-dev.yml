server:
  port: 8083

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/law_firm?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      database: 2
      password: 

# 日志配置
logging:
  level:
    com.lawfirm: debug
    org.springframework: warn

# Token配置
token:
  header: Authorization
  secret: abcdefghijklmnopqrstuvwxyz
  expireTime: 30

# 微信小程序配置
wx:
  miniapp:
    appid: ${WX_MINIAPP_APPID:your_appid}
    secret: ${WX_MINIAPP_SECRET:your_secret}
    msgDataFormat: JSON 