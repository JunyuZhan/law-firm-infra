-- Case模块职责说明脚本
-- 版本: V7000
-- 模块: Case
-- 创建时间: 2023-07-10
-- 说明: 本脚本不执行实际的数据库操作，仅用于记录模块职责说明

-- 注意：这是一个职责划分的说明文件，不对数据库结构进行任何更改

/*
Case模块职责说明：

1. Case模块负责的表：
   Case模块应负责所有案件相关表的创建和维护：
   - case_info：案件信息表
   - case_team：案件团队表
   - case_team_member：案件团队成员表 
   - case_participant：案件参与人表
   - case_task：案件任务表
   - case_document：案件文档表
   - case_event：案件事件表
   - case_fee：案件费用表
   - case_contract_relation：案件合同关联表
   - 其他与案件相关的表

2. 现有脚本说明：
   - V6001__init_case_tables.sql：创建案件相关表结构
   - V6002__init_case_data.sql：初始化案件相关基础数据

3. 后续开发规范：
   a) 表命名规范：
      - 所有案件相关表应遵循"case_xxx"命名规范
      - 表名应清晰表达表的业务含义
   
   b) 数据隔离：
      - 案件相关表必须包含tenant_id字段实现多租户隔离
      - 应设计适当的权限控制机制确保案件数据安全
   
   c) 关联规则：
      - 与其他模块的关联应通过ID引用实现，避免强耦合
      - 例如：通过client_id关联客户模块，通过employee_id关联人员模块
   
   d) 字典数据：
      - 案件特有的字典项应在Case模块内维护
      - 公共字典项应使用API层的sys_dict/sys_dict_item表

4. 开发建议：
   - 案件模块应专注于案件管理的核心业务逻辑
   - 应提供清晰的API供其他模块调用，避免业务逻辑分散
   - 案件相关统计和报表功能应在Case模块内实现
   - 大型文档内容应存储在文件系统，数据库仅存储元数据
*/

-- 此脚本不执行任何实际操作 