<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lawfirm.model.auth.mapper.RolePermissionMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.lawfirm.model.auth.entity.RolePermission">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="permission_id" property="permissionId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_id, permission_id, tenant_id, created_by, created_time
    </sql>

    <!-- 根据角色ID查询角色权限关联 -->
    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM auth_role_permission
        WHERE role_id = #{roleId}
    </select>

    <!-- 根据权限ID查询角色权限关联 -->
    <select id="selectByPermissionId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM auth_role_permission
        WHERE permission_id = #{permissionId}
    </select>

    <!-- 批量插入角色权限关联 -->
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO auth_role_permission (
            role_id, permission_id, tenant_id, created_by
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.permissionId}, #{item.tenantId}, #{item.createdBy})
        </foreach>
    </insert>

    <!-- 根据角色ID删除角色权限关联 -->
    <delete id="deleteByRoleId">
        DELETE FROM auth_role_permission
        WHERE role_id = #{roleId}
    </delete>

    <!-- 根据权限ID删除角色权限关联 -->
    <delete id="deleteByPermissionId">
        DELETE FROM auth_role_permission
        WHERE permission_id = #{permissionId}
    </delete>

    <!-- 根据角色ID和权限ID删除角色权限关联 -->
    <delete id="deleteByRoleIdAndPermissionId">
        DELETE FROM auth_role_permission
        WHERE role_id = #{roleId} AND permission_id = #{permissionId}
    </delete>

    <!-- 根据角色ID和权限ID列表删除角色权限关联 -->
    <delete id="deleteByRoleIdAndPermissionIds">
        DELETE FROM auth_role_permission
        WHERE role_id = #{roleId} AND permission_id IN
        <foreach collection="permissionIds" item="permissionId" open="(" separator="," close=")">
            #{permissionId}
        </foreach>
    </delete>
</mapper>
